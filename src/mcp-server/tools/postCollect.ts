/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postCollect } from "../../funcs/postCollect.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.PaymentRequest$inboundSchema.optional(),
};

export const tool$postCollect: ToolDefinition<typeof args> = {
  name: "post-collect",
  description: `Collect a payment from a phone number`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postCollect(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
