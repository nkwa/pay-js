/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { postDisburse } from "../../funcs/postDisburse.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.PaymentRequest$inboundSchema.optional(),
};

export const tool$postDisburse: ToolDefinition<typeof args> = {
  name: "post-disburse",
  description: `Disburse a payment from your balance to a phone number`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await postDisburse(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
