/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getPaymentsId } from "../../funcs/getPaymentsId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetPaymentsIdRequest$inboundSchema,
};

export const tool$getPaymentsId: ToolDefinition<typeof args> = {
  name: "get-payments-id",
  description:
    `Get the payment (collection or disbursement) with the id specified`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getPaymentsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
